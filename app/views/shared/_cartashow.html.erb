<div class="background-show">
    <div class="carta-back">
        <div class="tab-content" id="pills-tabContent">
            <div class="tab-pane fade show active" id="tapas" role="tabpanel" aria-labelledby="tapas-tab">
                <h2 style="text-align: center; padding-top: 10px; color: white;">Tapas</h2>
                <div class="separate">
                </div>
                <div class="">
                    <% @meals.each do |meal|  %>
                        <% if meal.tapa == true %>
                        <div class="name-price-carta meal-card">
                            <h5><%= meal.name %></h5>
                            <p class="mt-3"><%= meal.description %></p>
                            <h6 class="price"><%= meal.price%> €</h6>
                        </div>
                        <div class="separate">
                        </div>
                        <% end %>
                    <% end %>
                </div>
            </div>
            <div class="tab-pane fade" id="entrantes" role="tabpanel" aria-labelledby="entrantes-tab">
                <h2 style="text-align: center; padding-top: 10px; color: white;">Entrantes</h2>
                <div class="separate">
                </div>
                <div class="">
                    <% @meals.each do |meal|  %>
                        <% if meal.starter == true %>
                        <div class="name-price-carta meal-card">
                            <h5><%= meal.name %></h5>
                            <p><%= meal.description %></p>
                            <h6 class="price"><%= meal.price %>€</h6>
                        </div>
                        <div class="separate">
                        </div>
                        <% end %>
                    <% end %>
                </div>
            </div>
            <div class="tab-pane fade" id="principales" role="tabpanel" aria-labelledby="principales-tab">
                <h2 style="text-align: center; padding-top: 10px; color: white;">Platos</h2>
                <div class="separate">
                </div>
                <div class="">
                    <% @meals.each do |meal|  %>
                        <% if meal.main == true %>
                        <div class="name-price-carta meal-card">
                            <h5><%= meal.name %></h5>
                            <p><%= meal.description %></p>
                            <h6 class="price"><%= meal.price %>€</h6>
                        </div>
                        <div class="separate">
                        </div>
                        <% end %>
                    <% end %>
                </div>
            </div>
            <div class="tab-pane fade" id="carnes" role="tabpanel" aria-labelledby="carnes-tab">
                <h2 style="text-align: center; padding-top: 10px; color: white;">Carnes</h2>
                <div class="separate">
                </div>
                <div class="">
                    <% @meals.each do |meal|  %>
                        <% if meal.meat == true %>
                        <div class="name-price-carta meal-card">
                            <h5><%= meal.name %></h5>
                            <p><%= meal.description %></p>
                            <h6 class="price"><%= meal.price %>€</h6>
                        </div>
                        <div class="separate">
                        </div>
                        <% end %>
                    <% end %>
                </div>
            </div>
            <div class="tab-pane fade" id="pescados" role="tabpanel" aria-labelledby="pescados-tab">
                <h2 style="text-align: center; padding-top: 10px; color: white;">Pescados</h2>
                <div class="separate">
                </div>
                <div class="">
                    <% @meals.each do |meal|  %>
                        <% if meal.fish == true %>
                        <div class="name-price-carta meal-card">
                            <h5><%= meal.name %></h5>
                            <p><%= meal.description %></p>
                            <h6 class="price"><%= meal.price %>€</h6>
                        </div>
                        <div class="separate">
                        </div>
                        <% end %>
                    <% end %>
                </div>
            </div>
            <div class="tab-pane fade" id="postres" role="tabpanel" aria-labelledby="postres-tab">
                <h2 style="text-align: center; padding-top: 10px; color: white;">Postres</h2>
                <div class="separate">
                </div>
                <div class="">
                    <% @meals.each do |meal|  %>
                        <% if meal.dessert == true %>
                        <div class="name-price-carta meal-card">
                            <h5><%= meal.name %></h5>
                            <p><%= meal.description %></p>
                            <h6 class="price"><%= meal.price %>€</h6>
                        </div>
                        <div class="separate">
                        </div>
                        <% end %>
                    <% end %>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="navbar navbar-expand-sm navbar-light navbar-meals">
    <ul class="nav nav-pills tab-menu" id="pills-tab" role="tablist">
        <li class="nav-item col-4">
            <a class="nav-link active" id="own-events-tab" data-toggle="pill" href="#tapas" role="tab" aria-controls="tapas" aria-selected="true">Tapas</a>
        </li>
        <li class="nav-item col-4">
            <a class="nav-link" id="applied-events-tab" data-toggle="pill" href="#entrantes" role="tab" aria-controls="entrantes" aria-selected="false">Entrantes</a>
        </li>
        <li class="nav-item col-4">
            <a class="nav-link" id="favorites-tab" data-toggle="pill" href="#principales" role="tab" aria-controls="platos" aria-selected="false">Platos</a>
        </li>
            <li class="nav-item col-4">
            <a class="nav-link" id="favorites-tab" data-toggle="pill" href="#carnes" role="tab" aria-controls="carnes" aria-selected="false">Carnes</a>
        </li>
            <li class="nav-item col-4">
            <a class="nav-link" id="favorites-tab" data-toggle="pill" href="#pescados" role="tab" aria-controls="pescados" aria-selected="false">Pescados</a>
        </li>
            <li class="nav-item col-4">
            <a class="nav-link" id="favorites-tab" data-toggle="pill" href="#postres" role="tab" aria-controls="postres" aria-selected="false">Postres</a>
        </li>
    </ul>
</div>